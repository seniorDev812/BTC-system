{
  "name": "btc-options-simulator",
  "version": "1.0.0",
  "description": "BTC Options & Futures Trading Simulator with Delta Exchange Integration",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start": "cd backend && npm start"
  },
  "keywords": ["btc", "options", "futures", "trading", "simulator", "delta-exchange"],
  "author": "BTC Options Simulator",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
